import React from "react";
import ReactDOM from "react-dom/client";
import KeystrokeReflexEngine from "./components/KeystrokeReflexEngine.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <KeystrokeReflexEngine />
  </React.StrictMode>,
);
